# ZeroShare-E2EE-File-Transfe
Uses client-side AES-GCM 256-bit encryption in the browser. Files are encrypted before they are sent to the server. The decryption key remains in the URL hash fragment and is never sent to the backend.
